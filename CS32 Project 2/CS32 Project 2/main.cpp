//
//  main.cpp
//  CS32 Project 2
//
//  Created by Caden McQuillen on 4/18/19.
//  Copyright © 2019 Caden McQuillen. All rights reserved.
//


#include <iostream>
#include "Sequence.h"
using namespace std;

int main()
{
    /*
    Sequence s;
    s.insert(0,3);
    s.insert(1, 4);
    s.insert(2, 2);
    s.insert(3, 3);
    s.insert(4, 67);
  
    assert(s.find(3)==0); //checks that insert function works
    assert(s.find(2)==2);
    assert(s.find(67)==4);
    assert(s.size()==5); //checks size function works
    assert(s.insert(100, 6)==-1); //checks that insert wont work if position is bigger than size of sequence
    
    assert(s.set(2, 100)==true); //checks that set function works
    assert(s.find(100)==2); //checks that set function changes correct position
   
    unsigned long x;
    assert(s.get(0, x)  &&  x == 3); //checks get function works
    assert(s.get(1, x)  &&  x == 4);
    assert(s.get(4, x)  &&  x == 67);
    assert(!s.get(100, x) &&   x==x); //checks that get returns false if position > size and leaves sequence unchanged
    
    
    assert(s.erase(6)==false); //checks that erase return false if position > size
    assert(s.size()==5);
    

    assert(s.find(100)==2);  // checks that element 2 of the sequence is 100
    assert(s.erase(2)==true); // checks that erase works
 
    assert(s.find(100)==-1); // checks that erase removed 100 from sequence
 
    Sequence s2;
    
    s2.insert(0, 0);
    s2.insert(1, 0);
    s2.insert(2, 0);
    
    s.swap(s2);  // checking swap works properly
    assert(s.size()==3);  //swap switches the lengths
    assert(s2.size()==4);
    unsigned long y;
    assert(s.get(0, y)  &&  y == 0);  //switches all the values
    assert(s.get(1, y)  &&  y == 0);
    assert(s.get(2, y)  &&  y == 0);
    
    assert(s2.get(0, y)  &&  y == 3);  //switches all the values
    assert(s2.get(1, y)  &&  y == 4);
    assert(s2.get(2, y)  &&  y == 3);
    assert(s2.get(3, y)  &&  y == 67);
    
    
    Sequence s3; //  create sequence of size 5 with two values of 1
    s3.insert(0, 0);
    s3.insert(1, 1);
    s3.insert(2, 2);
    s3.insert(3, 1);
    s3.insert(4, 4);
    
    assert(s3.remove(1)==2); //check that remove function returns twice
    assert(s3.find(1)==-1); //checks that it removed all 1s
    assert(s3.size()==3); // checks that the size is now 3
    
    
    s2 = s3; //check the assignment operator
    assert(s2.size()==3);
    assert(s2.find(0)==0);
    assert(s2.find(2)==1);
    assert(s2.find(4)==2);

    Sequence s4 = s2;  //checks the copy constructor
    assert(s4.size()==3);
    assert(s4.find(0)==0);
    assert(s4.find(2)==1);
    assert(s4.find(4)==2);
    

    cerr<<"All tests passed"<<endl;
     
     
    
    Sequence s1;
    s1.insert(0, 30);
    s1.insert(1, 21);
    s1.insert(2, 63);
    s1.insert(3, 42);
    s1.insert(4, 17);
    s1.insert(5, 63);
    s1.insert(6, 12);
    s1.insert(7, 29);
    s1.insert(8, 8);
    s1.insert(9, 32);
    
    
    Sequence s2;
    
    
    s2.insert(0, 63);
    s2.insert(1, 17);
    s2.insert(2, 29);
    

    assert(subsequence(s1,s2)==5);
    
    Sequence s5;
    s5.insert(0, 1);
    s5.insert(1, 2);
    s5.insert(2, 3);
    s5.insert(3, 4);
    s5.insert(4, 5);
    
    Sequence s6;
    s6.insert(0, 9);
    s6.insert(1, 9);
    s6.insert(2, 9);
    
    Sequence result;
    result.insert(0, 10);
    result.insert(1, 11);
    result.insert(2, 12);
    
    interleave(s6, s5, result);
    
    result.dump();
    
     
    
    
    Sequence s;
    s.insert(0);
    s.insert(1);
    s.insert(3);
    s.insert(45);
    s.insert(10);
    s.dump();
    s.erase(4);
    s.dump();
    
    
    */

    
    
    
    
    
}

